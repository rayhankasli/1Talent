import { VacancyDetails.Model } from './vacancy-details.model';

describe('VacancyDetails.Model', () => {
  it('should create an instance', () => {
    expect(new VacancyDetails.Model()).toBeTruthy();
  });
});
