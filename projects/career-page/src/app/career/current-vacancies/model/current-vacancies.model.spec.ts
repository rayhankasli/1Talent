import { CurrentVacancies.Model } from './current-vacancies.model';

describe('CurrentVacancies.Model', () => {
  it('should create an instance', () => {
    expect(new CurrentVacancies.Model()).toBeTruthy();
  });
});
